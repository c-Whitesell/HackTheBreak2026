{
  "name": "helloworld",
  "displayName": "HelloWorld",
  "description": "Sticky Pinned Functions for Hackathon",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "enabledApiProposals": [
    "editorInsets"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pin.function",
        "title": "Pin Function",
        "icon": "$(pin)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "pin.function",
          "group": "navigation",
          "when": "editorTextFocus"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.16.0",
    "eslint": "^9.39.3",
    "typescript": "^5.7.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
